# Frontend build-time config.
# Leave UNSET for local dev (requests go to /api via the Vite proxy).
# For production (Vercel), set this to your deployed backend origin (NO trailing slash):
#   VITE_API_URL=https://jobsense-api.onrender.com
VITE_API_URL=
